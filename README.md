# Dependencies
hyprland
`hyprshot`,`hyprpicker`,`hyprsunset`,`hyprlock`,`hypridle`

terminal
`alacritty`

notification manager
`dunst`

screensharer
`hyprland-preview-share-picker` (AUR)

bar
`wayle`

app launcher
`tofi`

wallpapers
`awww`
# Keybinds
look at `keybinds.lua` in `hypr`

# Wallpapers
put the image files for them in `~/Pictures/wallpapers`, configurable in `hypr/change_wallpaper.sh`
